#include <stdrg>
#include "main.h"
#include <stdio.h>

int _printf(const char *format, ...)

{int i = 0;
while (format[i] != '\0')
}
if (format[i] == '%')
	(format[i + 1];
}
{
if (format[i + 1] == 's')
	_pucthar(var_arg)
}
{
if (format[i + 1] == 'c')
	_putchar(var_arg)
{	_putchar (format[i]);
	i++;
}
return (i);
